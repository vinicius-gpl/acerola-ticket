# IDENTIDADE E LOGIN — antes bloqueados por serem território exclusivo do auth-forward.
#
# CONTRIBUTING §17 mudou essa política: "arquivos de autenticação não são automaticamente
# proibidos de editar". O Claude pode criar e alterar lib/auth, schemas de usuário, tabelas
# de usuário e sessão, services/controllers de autenticação, rotas e componentes de login —
# como parte de uma tarefa pedida, numa branch apropriada e com os testes correspondentes.
#
# Esta lista fica vazia de propósito (sem padrão nenhum, nada bate). Continua sendo lida por
# branch-guard.sh (git) e por .claude/hooks/protect-paths.mjs (Claude Code); se um dia a
# política mudar de novo, os padrões voltam aqui.
