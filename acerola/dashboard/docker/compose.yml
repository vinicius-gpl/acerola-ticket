# Este compose sobe só a aplicação — um serviço, que responde `/api` e o SPA na mesma porta.
#
# O banco (Postgres, Neon) e os arquivos (Cloudflare R2) são remotos: não há volume de dados
# aqui. O container só precisa das variáveis do `server/.env` (DATABASE_URL, R2_*) pra subir —
# sem elas, o server recusa iniciar.
#
# Em produção fica atrás de um Traefik (ex.: Coolify), que resolve domínio e TLS;
# localmente é só `-p 3005:3005`.
services:
  app:
    build:
      context: ..
      dockerfile: docker/dockerfile
    env_file:
      - path: ../server/.env
        required: false
    ports:
      - '3005:3005'
