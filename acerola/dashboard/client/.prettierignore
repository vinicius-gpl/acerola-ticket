dist
coverage
storybook-static
src/routeTree.gen.ts

# Território do CLI (shadcn) — a seção 5 do CONTRIBUTING proíbe editar aqui, então
# também não cobramos o nosso estilo de formatação de um arquivo que a CLI sobrescreve.
src/lib/vendor
